# landing/ (retired)

The interactive marketing page that lived here is **retired**.

**Canonical site:** `site/` (Astro) → deploys to GitHub Pages:

https://ahmedragab20.github.io/diffing/

```bash
pnpm docs:dev      # from repo root
pnpm docs:build
```

Historical build notes (outdated product facts — do not trust versions/MCP counts):

- `BUILD-SPEC.md` — frozen archive of the old landing spec

The previous `index.html` / `styles.css` / `main.js` demo has been removed so
visitors are not served stale v0.2.1-era claims.
