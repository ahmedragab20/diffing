---
"diffing": patch
---

Reuse compatible CLI review sessions by default, require a healthy HTML review shell before reopening, preserve live records across transient health-probe failures, let the OS allocate ports atomically, and close listeners when startup cannot publish discovery state. Add `--new-session` for deliberate same-scope concurrency.
