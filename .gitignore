node_modules
dist
# .diffing is deliberately NOT here — nothing should ever be written into the consumer repo. If you see one, delete it.
.playwright-mcp
# Rust TUI build artefacts (workspace lives in ./crates)
target/
# OS / editor noise
.DS_Store
*.swp
.idea/
.vscode/
